# Craft API base URL.
# Get it from the Craft app: Imagine tab (sidebar) -> Settings -> API -> copy the API URL.
# The token is embedded in the URL path, so treat this whole value as a secret.
# Do NOT commit your real .env file.
CRAFT_API_BASE_URL=https://connect.craft.do/links/XXXXXXXX/api/v1
